import React from "react";

type SidebarProps = {
  currentScreen: string;
  playlistNames: string[];
};

export default function Sidebar({
  currentScreen,
  playlistNames,
}: SidebarProps) {
  return <div></div>;
}
